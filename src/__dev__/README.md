# 開發專用目錄

此目錄包含僅在開發環境中使用的組件和工具。

## 使用規則

1. **環境限制**：這些文件只應在 `import.meta.env.DEV === true` 時使用
2. **不打包到生產**：構建時應自動排除
3. **命名規範**：所有文件應以 `.dev.tsx` 結尾

## 包含內容

- 調試工具
- 測試頁面
- 開發輔助組件
- Mock 數據

## 注意事項

⚠️ 永遠不要在生產代碼中直接導入此目錄的文件！